# Netlify redirects for SPA (Single Page Application)
# Unmatched URLs fall back to shell.html, not index.html: index.html is now the
# prerendered home page, and serving it for /dashboard would flash the home page
# and claim the home page as canonical for every unmatched URL.

/*    /shell.html   200
