// Quick test to see generated tasks for White's Tree Frog
// Note: These modules may not exist yet
// import { generateCareTasks } from '../services/careTaskGenerator';
// import { getAnimalById } from '../data/animals';

// const profile = getAnimalById('whites-tree-frog');
// if (profile) {
//   console.log('=== NON-BIOACTIVE SETUP ===');
//   const nonBioTasks = generateCareTasks(profile, { isBioactive: false });
//   nonBioTasks.forEach(task => {
//     console.log(`${task.title} (${task.frequency})`);
//     console.log(`  ${task.description}`);
//   });
//
//   console.log('\n=== BIOACTIVE SETUP ===');
//   const bioTasks = generateCareTasks(profile, { isBioactive: true });
//   bioTasks.forEach(task => {
//     console.log(`${task.title} (${task.frequency})`);
//     console.log(`  ${task.description}`);
//   });
// }
